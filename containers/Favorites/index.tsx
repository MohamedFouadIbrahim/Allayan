import * as React from 'react';
import LazyContainer from '../../components/LazyContainer';

interface IFavoritesProps {
}

const Favorites: React.SFC<IFavoritesProps> = (props) => {
    return (
        <LazyContainer>

        </LazyContainer>
    )
};

export default Favorites;